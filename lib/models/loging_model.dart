class LogingModel {
  String? id;
  String? name;
  String? userName;
  String? phone;
  String? token;

  // Construct User
  LogingModel({this.id, this.name, this.userName, this.phone, this.token});
}
